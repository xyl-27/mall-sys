<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gec.dao.RegisterUserMapper">
    
    <!-- RegisterUser findByUsername(String username); -->
    <select id="findByUsername" parameterType="java.lang.String" resultType="com.gec.domain.entity.RegisterUser">
        SELECT rt_user_id as rtUserId, rt_username as rtUsername, password, created_at as createdAt, updated_at as updatedAt
        FROM tbl_register_user
        WHERE rt_username = #{username}
    </select>
</mapper>