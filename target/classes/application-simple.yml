server:
  port: 8090
  servlet:
    context-path: /mall-sys

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall?useSSL=false&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.gec.domain.entity
  # 不指定mapper-locations，完全不加载XML文件